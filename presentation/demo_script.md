# Demo Script

1. Run the Horspool sentence-mode demo:

```
python run_match.py TextFiles pattern.txt --algo horspool --mode sentence --threshold 0.7
```

2. Show `results.json` and discuss a matched sentence and an unmatched one.
